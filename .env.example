# =============================================================================
# Environment Variables Configuration for Image Scoop
# =============================================================================
#
# IMPORTANT: NAMING CONVENTION RULES
#
# Frontend variables (exposed to browser):
#   - MUST use REACT_APP_ prefix
#   - Accessible in React components via process.env.REACT_APP_*
#   - Bundled into frontend code and visible in browser
#   - ⚠️  NEVER put secrets here - they're publicly visible!
#
# Backend variables (server-side only):
#   - MUST NOT use REACT_APP_ prefix
#   - Only available in Netlify Functions and backend code
#   - Never exposed to the browser
#   - ✅ Safe for secrets and API keys
#
# =============================================================================

# -----------------------------------------------------------------------------
# Frontend Variables (REACT_APP_ prefix - Exposed to Browser)
# -----------------------------------------------------------------------------

# API Configuration
REACT_APP_API_URL=/.netlify/functions/process-image

# Firebase Client SDK (Public Firebase config - safe to expose)
REACT_APP_FIREBASE_API_KEY=your_firebase_api_key
REACT_APP_FIREBASE_AUTH_DOMAIN=your_project_id.firebaseapp.com
REACT_APP_FIREBASE_PROJECT_ID=your_firebase_project_id
REACT_APP_FIREBASE_STORAGE_BUCKET=your_project_id.appspot.com
REACT_APP_FIREBASE_MESSAGING_SENDER_ID=your_messaging_sender_id
REACT_APP_FIREBASE_APP_ID=your_firebase_app_id
REACT_APP_FIREBASE_DATABASE_URL=https://your_project_id-default-rtdb.firebaseio.com

# Google Analytics 4
REACT_APP_FIREBASE_MEASUREMENT_ID=G-XXXXXXXXXX

# -----------------------------------------------------------------------------
# Backend Variables (NO prefix - Server-Side Only)
# -----------------------------------------------------------------------------

# Cloudflare R2 Storage (Backend credentials)
R2_ACCOUNT_ID=your_cloudflare_account_id
R2_ACCESS_KEY_ID=your_r2_access_key_id
R2_SECRET_ACCESS_KEY=your_r2_secret_access_key
R2_BUCKET_NAME=your_r2_bucket_name

# Cloudflare Images (Used by upload-backgrounds.js script)
CLOUDFLARE_ACCOUNT_ID=your_cloudflare_account_id
CLOUDFLARE_API_KEY=your_cloudflare_api_key

# Firebase Admin SDK (Server-side credentials for Netlify Functions)
FIREBASE_PROJECT_ID=your_firebase_project_id
FIREBASE_CLIENT_EMAIL=your-service-account@your-project.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour_private_key_here\n-----END PRIVATE KEY-----\n"
FIREBASE_DATABASE_URL=https://your_project_id-default-rtdb.firebaseio.com

# -----------------------------------------------------------------------------
# Optional / Future Variables
# -----------------------------------------------------------------------------

# Stripe Payment Processing (if implementing payments)
# REACT_APP_STRIPE_PUBLISHABLE_KEY=pk_test_...  # Frontend - public key
# STRIPE_SECRET_KEY=sk_test_...                 # Backend - secret key
# STRIPE_WEBHOOK_SECRET=whsec_...               # Backend - webhook signature

# Sentry Error Tracking
# DSNs are public identifiers used to route errors to your Sentry project
# Frontend DSN (exposed to browser)
REACT_APP_SENTRY_DSN=https://836ef0c8872d0abfc75188d0fb481f47@o4509055999541248.ingest.us.sentry.io/4510621343875072
# Backend DSN (server-side scripts and functions)
SENTRY_DSN=https://836ef0c8872d0abfc75188d0fb481f47@o4509055999541248.ingest.us.sentry.io/4510621343875072

# -----------------------------------------------------------------------------
# Setup Instructions
# -----------------------------------------------------------------------------
#
# 1. Copy this file to .env:
#      cp .env.example .env
#
# 2. Fill in your actual values in .env
#    ⚠️  NEVER commit .env to git - it contains secrets!
#
# 3. For local development with Netlify Functions:
#      netlify dev
#
# 4. Deploy environment variables to Netlify (after editing .env):
#      netlify env:import .env
#
# 5. Verify in Netlify dashboard:
#      https://app.netlify.com/sites/YOUR_SITE/settings/deploys#environment
#
# -----------------------------------------------------------------------------
# Security Checklist
# -----------------------------------------------------------------------------
# ✅ .env file is in .gitignore
# ✅ No secrets have REACT_APP_ prefix
# ✅ All REACT_APP_ variables are safe to expose publicly
# ✅ Backend credentials use NO prefix
# ✅ Never commit .env file to repository
# ✅ Rotate keys immediately if accidentally exposed
